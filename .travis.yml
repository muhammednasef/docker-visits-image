sudo: required
services:
  - docker

before_install:
  - docker-compose up --build -d

script:
  - docker run visits-node npm run test

# deploy:
#   provider: heroku
#   api_key: 'kererer'
#   on:
#     branch: master
